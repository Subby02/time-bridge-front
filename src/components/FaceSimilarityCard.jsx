import styles from './MissingCard.module.css'
import { Link } from 'react-router-dom';

export default function FaceSimilarityCard({ originalImage, genImage, name, gender, birth, place, date }) {
  return (
    <div>

    </div>
  );
}